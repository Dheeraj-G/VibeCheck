{
  "name": "vibecheck-monorepo",
  "version": "1.0.0",
  "description": "VibeCheck - Music recommendation app with Spotify OAuth",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}
